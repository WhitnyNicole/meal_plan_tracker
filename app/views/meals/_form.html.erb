<%= form_for [meal.meal_plan, meal]  do |f| %>

<%= render partial: 'layouts/errors', locals: {object: @meal} %>

  <%= f.label :food %>
  <%= f.text_field(:food) %>

  <%= f.label :day %>
  <%= f.date_field(:day) %>
  <br><br>

  <%= f.label :meal_plan_id, "Select a Meal Plan"%>
  <%= f.collection_select :meal_plan_id, MealPlan.all, :id, :goal, prompt: true %>

  <br><br>
  <%= f.submit %>
<% end %>
