<%= render "shared/page_title", title: "Add Your Meals" %>

<h4> Adding to Meal Plan: <%= @meal.meal_plan.try(:goal) %> (<%= @meal.meal_plan.try(:description) %>) </h4>
<%= render 'form', {meal: @meal} %>
